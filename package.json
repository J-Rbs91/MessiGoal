{
  "name": "messigoal",
  "version": "1.0.0",
  "description": "Base de données participative recensant tous les buts de Lionel Messi (site statique GitHub Pages).",
  "scripts": {
    "build": "node scripts/build-data.js",
    "build:check": "node scripts/build-data.js --check",
    "dev": "node scripts/build-data.js && node scripts/dev-server.js",
    "test": "node test/build-data.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
